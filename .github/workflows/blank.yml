name: Hello World Workflow
on: 
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - src/*.txt

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Linting the code
        run: echo ${{runner.os}}